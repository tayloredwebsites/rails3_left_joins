<%= form_for(@fk_reference) do |f| %>
  <% if @fk_reference.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@fk_reference.errors.count, "error") %> prohibited this fk_reference from being saved:</h2>

      <ul>
      <% @fk_reference.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
	<%= f.label :lookup_id %><br />
	<%= f.select(:lookup_id, options_from_collection_for_select(@lookups, "id", "name")) %><br />
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_field :description %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
